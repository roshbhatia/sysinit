-- VSCode configuration for init-vscode generation

return {
  modules = {
    editor = { "vscode" },
    ui = {},
    tools = {},
  },
  annotations = {
    -- Add annotation patterns if needed for module processing
  },
}