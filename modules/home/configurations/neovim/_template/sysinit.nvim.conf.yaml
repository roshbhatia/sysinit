# Example Neovim sysinit config
# Copy to home dir if needed
copilot:
  enabled: true  # Set to false to disable Copilot globally

avante:
  provider: copilot
  providers:
    copilot:
      model: gpt-4.1
    # Add additional providers as needed:
    # ollama:
    #   model: llama2

