import QtQuick
import qs.Theme

Text {
    text: "\uf1105" // 󱄅 Nix snowflake
    color: Theme.blue
    font.family: Theme.iconFont
    font.pixelSize: Theme.iconSize + 2
    verticalAlignment: Text.AlignVCenter
}
