# flatten-review-to-one-session

One diff session at a time over a flat list of changed files, so the review reads the same in a single repository and in a workspace of forty, and knows the name of no session manager.
