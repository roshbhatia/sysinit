## REMOVED Requirements

### Requirement: aider suppresses commit authorship attribution

**Reason**: The aider config that set `attribute-co-authored-by` and `attribute-author` is deleted.

**Migration**: None. The no-co-author posture is unchanged for every harness that remains; each enforces it in its own config rather than through this requirement.
