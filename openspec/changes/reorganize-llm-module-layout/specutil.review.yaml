version: 1
change: reorganize-llm-module-layout
decision: approved
note: 'Phase 1 complete after adversarial review rounds 1 and 2 (K=2). Six defects found and fixed, five mutation tested. Artifacts corrected: false removal criterion, unauthorized-edit criterion, unsatisfiable old-path criterion, two count errors, missing import-depth tasks.'
change_hash: 31ac87ab45cdda0a
base_commit: 7a490c231a9781b34e58db10d016b49c79f8830d
items:
    1c3cbfec1de2b9d3:
        hash: a7ea4351b6b0b613
        text: Move the shellcheck `require_nonempty` canary from `llm/config` to `llm/runtime`, so the check still fails loudly if this subtree stops contributing scripts
    02af5a096b238a5c:
        hash: e0408ec39df3cbc0
        text: Rename gemini's wrapper derivation from `devin-guard` to `gemini-exit-code-guard`, so two harnesses stop building two different derivations under one name
    02f91804907fdfb2:
        hash: 95f1f892e92a7da7
        text: 'Pilot: create `harnesses/default.nix` holding the `imports` list (follows the aggregating list in `llm/default.nix`), move `config/amp.nix` to `harnesses/amp.nix`, and point `llm/default.nix` at `./harnesses`. One flat harness proves the aggregator and the `../lib` import depth before the other ten move'
    6f86bd93a444e3a1:
        hash: b4dd788c1638f852
        text: 'Create `harnesses/pi/`: `default.nix` from `config/pi.nix`, `settings-keys.nix`, `vendored-extensions.nix`, `shell-prefix.sh`, all four files under `extensions/` (two loose `.ts` files plus the two-file `openspec-sidebar/`), and the ten lockfiles under `locks/`. Correct the library import depth to `../../lib`, repoint the three internal reads that the renames break (`settings-keys.nix`, `vendored-extensions.nix`, `shell-prefix.sh`), and keep the sidebar''s `source` a directory rather than expanding it to files'
    7a0f4eb84383a703:
        hash: 43d43ea1c16a50af
        text: 'Repoint the remaining `flake.nix` sites: the `pi-shell-prefix`, `pi-settings-keys`, `pi-vendored-extensions`, and `opencode-render` checks, and add `llm/harnesses` to the shellcheck canary'
    7ab950ba0ab40791:
        hash: d1c30623e427b43f
        text: 'Confirm: the owner reads the file-level difference between the current generation''s result path and the new one, and decides whether every difference was intended. The expectation is that no file appears and no file disappears, and that the only content difference is the `citelock` store path, which moves because the script''s usage comment was corrected. Any appearing or disappearing file is a defect in an earlier phase, not an expected consequence of this one'
    7ce2adf8d82bc7af:
        hash: 8cf307adda8dc2d7
        text: 'Confirm: the owner runs one agent session and reports whether the notifier, the statusline, and the state bus still fire, which is the judgment no check in this change can make'
    7d7117f913c15b8b:
        hash: d549ed4aa8b807e1
        text: 'Adversarial review (`adversarial-review` skill): critics attempt to break the skill-tools phase against the proposal `Behavior` criteria, in particular the criterion that no skill tree gains a duplicate script; revise until the loop reaches a terminal state (see the skill for the scaled round cap)'
    7ea79dce9db2cf6e:
        hash: 4c9eecb796bdd178
        text: Move `llm/mcp.nix` to `lib/mcp-catalog.nix`, repoint `lib/harness-kit.nix` at it, and leave `lib/mcp.nix` alone. The two are the catalog and the per-harness formatters, so they keep distinct names
    7fffa2f20cae7345:
        hash: 8117ab3c46905803
        text: Confirm the check from 5.1 now passes, then mutation test it. Revert 5.2 alone and confirm the destructive case fails. Then break the allowed case alone and confirm that case fails. A check that passes for one reason proves one thing
    8d7ff552710bcbcb:
        hash: 760446a55094f495
        text: 'Create `harnesses/claude/`: `default.nix` from `config/claude.nix`, `nix-guard.sh` from `config/claude-nix-guard.sh`, `statusline.sh`, and `worklog-hook.py` from `config/worklog.py`. Correct the library import depth to `../../lib`'
    8e19e45204ff46dc:
        hash: 2725e3d4459a0ae0
        text: 'Write the failing check first: a `flake.nix` check that pipes hook JSON carrying a destructive command to devin''s and gemini''s exec-guard wrappers and asserts a non-zero exit, plus JSON carrying an allowed command and asserts exit 0 (follows `destructive-guard-fixtures`, which drives the assembled guard rather than the source file). Confirm it fails against the current script'
    9e71dff4fbee6066:
        hash: b40838604f2c2a2b
        text: 'Round 2 finding: replace the two-filename denylist in `skill-render-shape` with the list in `skills/tool-sources.nix`, which `skill-tools.nix` also builds from, so a third skill-owned CLI cannot escape the guard. Mutation tested by adding a third entry pointing at an installed path'
    23b3aa7c0002fefa:
        hash: bb66ee890b04c76a
        text: Run `nix flake check` and confirm it exits 0. Confirm no top-level file under `llm/` shares a name with a directory beside it
    35a328ffd1291c7f:
        hash: f60b62b3c8e7bdea
        text: 'Adversarial review (`adversarial-review` skill): critics attempt to break the library-path phase against the proposal `Behavior` criteria and against D5; revise until the loop reaches a terminal state (see the skill for the scaled round cap)'
    46c14f4213e2ea25:
        hash: d4b9c8c1f83fd828
        text: Move `llm/skills.nix` to `skills/render.nix`, and correct its relative reads of the registry and of `lib/vocab.nix`
    80cf7433bc7b752c:
        hash: 1c56ec2e51f4538e
        text: Add `modules/home/programs/llm/skill-tools.nix` building `citelock` from the new path (follows the derivation in `citation-tools/default.nix`, which this replaces), and import it from `llm/default.nix`
    085befd7500e6d92:
        hash: 35c68a82f3d57817
        text: 'Move the remaining eight `agent-*.sh` scripts into `runtime/`, plus `loop-gate.sh`, which is a gate rather than an agent script: `agent-notify.sh`, `agent-identity.sh`, `agent-state.sh`, `agent-prompt.sh`, `agent-focus.sh`, `agent-review.sh`, `agent-review-suffix.sh`, and `agent-busy-panes.sh`'
    88fa6f5df54289b6:
        hash: 905b199ab0f67fa7
        text: 'Delete `modules/home/programs/llm/config/`, then confirm that no reference to that directory survives outside `openspec/`. Everything under `openspec/changes/` and `openspec/specs/` is exempt and stays untouched: both are history, and `openspec/specs/` alone holds 8 such references'
    91cf027794566ef3:
        hash: ee9d0d22550e9afd
        text: Repoint the pre-commit hook in `.githooks/` at the same path, then check that `rg citation-tools` over the tracked tree finds nothing outside `openspec/changes/`. This includes the usage comment inside `citelock.sh` itself, which named the deleted directory. That edit is authorized here, and it is why `bin/citelock` changes store path
    94b83c970c3e6d4d:
        hash: f85dac2ca9b9f4b6
        text: Change `runtime/exit-code-guard.sh` to call the guard through an injected absolute path instead of the bare name `claude-bash-guard`
    97cf4cc8f6c0ba52:
        hash: e643fe7a2ad64d78
        text: 'Move the two cross-harness output modules to the module root, not into `harnesses/`: `config/acp.nix` becomes `llm/acp.nix`, and `config/mcp-servers.nix` becomes `llm/mcp-servers.nix`. Both sit beside the `options.nix` that declares the option the second one sets'
    403bd3c605866a9d:
        hash: e09a67b296ea9123
        text: 'Create `harnesses/opencode/`: `default.nix` from `config/opencode.nix`, `render.nix` from `config/opencode-render.nix`, and `plugins/sysinit-notify.ts`. Correct the library import depth to `../../lib` in BOTH files, and repoint `default.nix` at `./render.nix`'
    547b5b9ae8235a0a:
        hash: 5be56dcbd0035fab
        text: 'Pilot: create `modules/home/programs/llm/runtime/` and move `config/agent-group.sh` into it. That script is sourced by `notify.nix`, by `agent-review-suffix.sh`, and by two `flake.nix` checks, so it exercises every reference kind in this phase on one file'
    586dcf86727ea564:
        hash: f3a0f2f64ab7369b
        text: Inject that path in the devin and gemini wrapper derivations, the way `runtime/default.nix` already injects `NOTIFY_EXE` and `SY_REAL`
    589f4f53643f8342:
        hash: 7e953737544ac4a6
        text: 'Repoint `hack/update-pi.sh`: `PI_NIX`, `LOCKS_DIR`, and the three path strings in its printed instructions'
    2045b623dd12333a:
        hash: 8787812419c4dc32
        text: Record the old-path to new-path map in this change's directory, so the seven in-flight changes that name old paths can be read against it
    5259b438e1c36339:
        hash: 95db6bcc3b4967f6
        text: Repoint `lib/guards.nix` at `../runtime/bash-guard.sh`, then confirm that no file under `lib/` reads a path outside `lib/` and `runtime/`
    33663abac47b2f02:
        hash: df2227d0bd18cb39
        text: 'Adversarial review (`adversarial-review` skill): critics attempt to break the harness-layer phase against the proposal `Behavior` criteria and against D2 and D8, in particular the criterion that no file under `harnesses/` imports the instructions, catalog, or renderer module directly; revise until the loop reaches a terminal state (see the skill for the scaled round cap)'
    48978de5488a4bf5:
        hash: 40d0b7cc988a75ac
        text: Run `nix flake check` and confirm it exits 0
    56691d191069e6e6:
        hash: 2edf3f0ba19c6161
        text: 'Apply: `nh darwin switch`, gated on `nix flake check` and `nh darwin build` exiting 0 and on 6.3 being confirmed'
    0903459dee996ca5:
        hash: 14051b5006ebe1f9
        text: 'Repoint the two harness modules that read the exit-code guard body: `config/devin.nix` and `config/gemini.nix`'
    79146888e7867b87:
        hash: 98985ee6c393a796
        text: Delete `modules/home/programs/llm/citation-tools/`
    a2aafb6a35198019:
        hash: 6eb8e37d426e86cb
        text: 'Repoint `flake.nix`: the `notifyIcons` and `agent-review-readiness` imports of `notify.nix`, and the `cfg=` prefix in the `agent-review-readiness` and `notify-defect-regressions` checks'
    a4e341f0dc43cc7c:
        hash: 1261c167d4811a10
        text: Move `config/wtrun.sh` to `skills/wtrun/wtrun.sh` with `git mv`, move the `wtrun` derivation out of `config/notify.nix` into `skill-tools.nix`, and drop `notify.wtrun` from the `home.packages` list in `llm/default.nix`
    a8b1531c327cf3b0:
        hash: 9ee0b388df2b187f
        text: 'Create `harnesses/cursor/`: `default.nix` from `config/cursor.nix`, plus `rules/nix.mdc` and `rules/markdown.mdc` from `config/cursor-rules/`. Correct the library import depth to `../../lib`'
    a80f214865a6b0f1:
        hash: 9878fd3c015b230a
        text: 'Pilot: move `citation-tools/citelock.sh` to `skills/citation-verification/citelock.sh` with `git mv`. The pilot is the move whose consumer set is largest, so a wrong assumption about who reads the script surfaces on one file rather than two'
    b9e046fe86228c02:
        hash: 6523ddf232b57c23
        text: 'Adversarial review (`adversarial-review` skill): critics attempt to break the guard fix against the proposal `Behavior` criteria and against D6 and D7, in particular whether the fix turns a fail-open guard into an over-blocking one; revise until the loop reaches a terminal state (see the skill for the scaled round cap)'
    be9a3b7beb35d229:
        hash: 493ce814716675fe
        text: 'Move the remaining flat harness modules into `harnesses/`: `codex.nix`, `copilot-cli.nix`, `crush.nix`, `devin.nix`, and `goose.nix`'
    c5bc95cbdabc66df:
        hash: b206dcfe16560a82
        text: Move `config/notify.nix` to `runtime/default.nix`, keeping its `{ pkgs, lib }` calling convention, and update its relative reads to the moved script names
    c28d3f888a177ecc:
        hash: 640c726824c7f09a
        text: Move `config/sy-gate.sh` to `runtime/sy-gate.sh`
    c92b96a7d37fbc82:
        hash: dfc73bf07f43c910
        text: 'Verify the new placement adds no script copy to the installed set: evaluate `skillExtraFiles` and confirm it still holds only the `adversarial-review` reference and `worklog/scripts/worklog-query.sh`. Neither moved script was installed before, so this is prevention, and the evaluation alone cannot tell prevention from a no-op. Task 1.11 supplies the check that can'
    c389951c57069cda:
        hash: 0227b441726ed4eb
        text: 'Update the shipped path text that teaches where harness config lives: `harnesses/cursor/rules/nix.mdc` and `AGENTS.md`'
    ca964b7974c91ffa:
        hash: 450cc2e92deb2681
        text: Record in `skills/default.nix` that a skill's top-level file is not installed and a subdirectory's contents are, because D4 now depends on that distinction
    daf696625df966de:
        hash: 5b31ee51574a8531
        text: 'Adversarial review (`adversarial-review` skill): critics attempt to break the runtime phase against the proposal `Behavior` criteria and against D1, in particular the claim that no file under `lib/` reads a harness path; revise until the loop reaches a terminal state (see the skill for the scaled round cap)'
    e1f00d0764232787:
        hash: 40d0b7cc988a75ac
        text: Run `nix flake check` and confirm it exits 0
    e5ca8e85322d21fc:
        hash: 85e2723e0fc935cc
        text: 'Repoint every importer of the two moved files: `llm/default.nix`, `lib/harness-kit.nix`, and the two `flake.nix` sites'
    e05badfb0547cb15:
        hash: f09ef358daab0859
        text: 'Round 1 finding: give the `citelock` flake check fixtures, because no change ships a `citations.lock` and the gate certifying this phase never executed the script. Assert exit 0 for a lockless directory and non-zero for a record missing required fields. Add a shellcheck `require_nonempty` canary for `llm/skills`, which now holds shell scripts and had none'
    e9f457e8e58041f0:
        hash: b5c81e3570f2ad2f
        text: 'Round 2 finding, fix-induced: the round-1 fixtures asserted an exit code only, so dropping `pkgs.jq` left both green while the format lint never ran. `require_tool jq` also dies with status 1. Assert the reason instead: the lockless case must report the no-op path, and the bad-lock case must name record `unanchored` in a `format:` message. Mutation tested by removing `pkgs.jq`'
    e46a45252701ba90:
        hash: 5fcfad7eb89e82cb
        text: Repoint the `flake.nix` citation gate at the new script path
    ebe3ea3ac2d8d421:
        hash: 28b2f0fb34132abc
        text: 'Build only: `nh darwin build`, which writes no system change'
    ebe94c802b532d17:
        hash: 9e638478ec1750df
        text: 'Update the two comments that name the old notifier path: `modules/home/programs/zsh/integrations/seshy-wezterm.zsh` and `overlays/inputs.nix`'
    ed09b1a36862261a:
        hash: 3f6295e292d795b8
        text: 'Create `harnesses/gemini/`: `default.nix` from `config/gemini.nix`, plus `extensions/openspec-awareness/CONTEXT.md` from `config/gemini-extensions/`. Correct the library import depth to `../../lib`'
    ee4c9cd4a9e3ac93:
        hash: 9ff661bff3d8c317
        text: Run `nix flake check` and confirm it exits 0. Read `git diff -M --stat` and confirm each moved harness asset is reported as a rename
    ef1dbff4e466cdcd:
        hash: 608a29efad6816fc
        text: Repoint the `bridgeArtifacts` paths in `runtime/default.nix` at the pi and opencode bridge files in their new homes
    f5f068054048fa5d:
        hash: f6c33f8686bd992b
        text: Move `config/claude-bash-guard.sh` to `runtime/bash-guard.sh`, and move `config/devin-guard.sh` to `runtime/exit-code-guard.sh`. Both names drop the harness prefix, because three harnesses share the first body and two share the second
    f50c93079389d32d:
        hash: 1bb0f6ea771b0b9b
        text: 'Round 1 finding: extend the `skill-render-shape` flake check to fail when a skill-owned CLI source appears in the installed set, so D4 is gated rather than only commented. Mutation test it by naming an installed path in the guard and confirming the check fails'
    f90a4db8b3630a8f:
        hash: 31491a00986012db
        text: Run `nix flake check` and confirm it exits 0. Read `git diff -M --stat` and confirm every file in this phase is reported as a rename, except `lib/guards.nix`, `flake.nix`, and the two harness modules
