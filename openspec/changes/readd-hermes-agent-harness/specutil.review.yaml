version: 1
change: readd-hermes-agent-harness
decision: approved
note: owner approved in session 2026-08-10 after being shown the external_dirs substitution to task 2.4; critics not run at owner request
change_hash: be86b31265e387c7
base_commit: 031d420e033ee3f342584ebda494431b48847e4e
items:
    2b5d605ce0d90699:
        hash: 7b5cd30b29878ba0
        text: Record the closure-size delta and wall-clock build time for the darwin configuration, so the cost is a number the owner can price rather than an impression
    3d2871bc9f482c96:
        hash: 17b0771257f04415
        text: Add the `hermes-agent` flake input with its own nixpkgs pinned to a revision, following the `hunk` input's form in `flake.nix`; read upstream's `flake.lock` for the revision they test against
    5b7a3ac38f224ec0:
        hash: 14dbd32f494290ae
        text: 'Adversarial review (`adversarial-review` skill): critics attempt to break the package phase against the proposal `Behavior` criteria; revise until the loop reaches a terminal state (see the skill for the scaled round cap)'
    7eba96a7c7be96c9:
        hash: 851de825da891bb6
        text: Write `overlays/hermes-agent.nix` over `packages.${system}.minimal` with `extraDependencyGroups = [ "anthropic" ]`, re-wrapping `hermes`, `hermes-agent`, and `hermes-acp` with a prefixed subagent PATH, and register it in `overlays/default.nix`
    17bc9a868ed93193:
        hash: ed3581531ad74d7c
        text: 'Adversarial review (`adversarial-review` skill): critics attempt to break the editor phase against the proposal `Behavior` criteria; revise until the loop reaches a terminal state'
    53a251630d2b06d0:
        hash: 046edebda373df3b
        text: 'Confirm: the owner accepts the measured build time and closure delta from 1.3, given that upstream classes the Nix path as best-effort and nothing substitutes this derivation'
    73e472fc94af79a6:
        hash: 726764fe16550be2
        text: Build both configurations, then parse the rendered `~/.hermes/config.yaml` with `yq` and compare its `mcp_servers` keys against the catalog minus `suppressedServers`
    30554cb98b72b65e:
        hash: e6102ea23741685b
        text: 'Apply: `git push`, then `nh darwin switch` from the `sysinit.laurel` checkout, gated on `nix flake check` and `nh darwin build` exiting 0'
    45112b7e6cf5077c:
        hash: 024afcbb68333db6
        text: Add the `hermes` entry to `harnesses/registry.nix` with `package = "hermes-agent"`, `context`, `skillLoader`, `ownIcon`, `notify`, `bridge`, and `neovimAdapter` filled in per the proposal
    abadc2d7c579876f:
        hash: d7df78fbdfa2b02f
        text: 'Run the post-switch checks: `hermes --version`, `hermes-acp --help`, the credential grep over the built store path, and a hermes session from `env -i PATH=/usr/bin:/bin` resolving the six subagent binaries'
    b89a447482d272be:
        hash: aacc52a5bbec1b9b
        text: Declare `skills.external_dirs` in the managed config so hermes reads `~/.claude/skills` in place, and do NOT restore the deleted renderer; its `<category>/<name>/SKILL.md` layout and `metadata.hermes.category` key are not what the pinned version reads
    b7749d90a4dc3ffe:
        hash: 4b6455c33f5d7561
        text: 'Confirm: the owner runs `hermes` once and decides whether the keys in `~/.hermes/config.yaml` are the ones they meant this repository to declare'
    bf1ab0c9b92fedad:
        hash: b064758d31b23103
        text: Write `harness/adapters/hermes.lua` following `adapters/goose.lua`, and add `hermes` to the `ORDER` list in `harness/registry.lua`
    c9a8ec1eee55d017:
        hash: 1658001dea29b5a7
        text: 'Adversarial review (`adversarial-review` skill): critics attempt to break the module phase against the proposal `Behavior` criteria; revise until the loop reaches a terminal state'
    c57d016dd4075efc:
        hash: c168352fd2e22928
        text: Restore `formatForHermes` in `lib/mcp.nix` beside the nine existing formatters, emitting `command`/`args` for stdio servers and `url` for http servers
    ca043821eed40c2a:
        hash: 040c114985c703d4
        text: Build both configurations, run the headless neovim check that `get_by_name("hermes")` returns a table, then inject `neovimAdapter = "nope"` and require the darwin build to exit non-zero with the assertion's own message
    da3318444e454996:
        hash: 06c1a3d2b4cffa12
        text: Write `harnesses/hermes.nix` following `harnesses/goose.nix`, owning `~/.hermes/config.yaml` through `sysinit.llm.managedFiles` with `format = "yaml"`; the deleted `yq` activation script MUST NOT come back
    e9172479776ca3b3:
        hash: 2fb69800c7ceda29
        text: Add the evaluation-time assertion in `runtime/default.nix` that every registry `neovimAdapter` value names an existing file under the neovim adapters directory
    ecdeee8262ccda15:
        hash: e3a218f1615d0aa1
        text: Build both configurations, then grep `nix-store --query --requisites` over each result for `torch`, `faster-whisper`, `ctranslate2`, and `onnxruntime`; a hit means `minimal` is not the output that got consumed
