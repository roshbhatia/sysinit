version: 1
change: decompose-flake-checks
decision: approved
note: Relocation phase landed and verified by derivation path (18/19 identical, the exception being the check whose src is the flake source). Shell-body extraction deliberately deferred and recorded.
change_hash: 7e67f7cda0e03b28
base_commit: 3da51997f0f6ed9da9e19bf8720b1a408c740b7f
items:
    2defa399acd57c42:
        hash: f0c3e489c60e6ac3
        text: 'Move the remainder: `schema-templates-conform`, `zsh-fragments-parse`, `lua-parses`, `openspec-default-schema`, `pi-no-theme-writer`, `pi-shell-prefix-loads-aliases`, and `llm-asset-paths-resolve`'
    3f9291d768aeca29:
        hash: 1dd7ed05e462a52f
        text: Move `managed-file-reconcile` (274 lines) and `managed-file-merge3` (81), which share a subject and a prelude
    6ed6b5c4bf7e2bc7:
        hash: fba40621e1d79a7d
        text: Format `flake.nix` and every new file with `nix fmt`, in its own commit, which also clears the pre-existing unformatted region
    9a1247bc0b762ef2:
        hash: bf1d9327bfa1ae4f
        text: Run `nix flake check`, and confirm the shellcheck gate now reports the extracted script among the files it checked
    34a8a84ffd9cd351:
        hash: 10d714efa39c7ae1
        text: 'Adversarial review (`adversarial-review` skill): critics attempt to break the bulk move against the proposal `Behavior` criteria, in particular whether any assertion changed polarity or any store path arrived differently; revise until the loop reaches a terminal state (see the skill for the scaled round cap)'
    047b37143b0d3b3f:
        hash: dabfc770da7d3637
        text: Remove the emptied checks region from `flake.nix` and confirm the file is under 300 lines with no Nix string block longer than 10 lines
    488fd1e23ad8b892:
        hash: facec2fbc3c21547
        text: 'Adversarial review (`adversarial-review` skill): critics attempt to break the reduction against the proposal `Behavior` criteria and `Non-goals`, in particular whether anything outside the checks region changed behavior; revise until the loop reaches a terminal state (see the skill for the scaled round cap)'
    689ec1bf107a4772:
        hash: d2cc0cb1c814ade0
        text: Add `checks/lib/prelude.sh` declaring `note`, `fail`, `expect_rc`, and `expect_out` once, reconciling the variants that differ today
    7860a8d8c7ba892b:
        hash: 047d70269e6afd7f
        text: 'Pilot: move `agent-review-readiness`, the largest at 388 lines, with its body extracted to `checks/agent-review-readiness.sh` and store paths passed as environment variables. Confirm its `drvPath` is unchanged'
    49854bcd2ea19b5c:
        hash: 646522453b8002f1
        text: Move `notify-defect-regressions` (171 lines), which carries two roots (`$cfg` and `$harness`) and the `require_file` guards
    08495350a98274b8:
        hash: 40491a9d8aa8f292
        text: Run `nix flake check` and re-confirm all 19 `drvPath` values against the baseline
    46812612101ebb46:
        hash: 235cf9cd72c204b4
        text: Add `checks/default.nix` taking one attrset and returning the checks attrset, and have `flake.nix` call `import ./checks { ... }` (follows `modules/lib/default.nix` and `harnesses/default.nix`)
    ace3c0b08cca2173:
        hash: 37a70cfab3c10a4a
        text: Move `destructive-guard-fixtures` (118) and `exit-code-guard-blocks`, which share the guard subject
    aea8f18a8f2208fb:
        hash: 4d0c1ec7a1da0a11
        text: Move `cacheBundleFor` out of the checks region to where packages are built, since it is a packages helper and not a check
    c3ffcca770bde4a9:
        hash: 445767404ed0bb16
        text: Move `citelock` (82), `skill-render-shape` (66), `pi-settings-keys-exist` (69), and `opencode-config-schema` (62)
    ca08ec385f3cd208:
        hash: 88691e3d2a0b317c
        text: 'Adversarial review (`adversarial-review` skill): critics attempt to break the pilot against the proposal `Behavior` criteria and D2 and D4, verifying by derivation path rather than by reading the diff; revise until the loop reaches a terminal state (see the skill for the scaled round cap)'
    cfcc8c84499822ae:
        hash: 7b13eda19a0f6a02
        text: Add a `require_nonempty` canary for `checks/` to the shellcheck gate, so the gate fails loudly if the directory stops contributing scripts
    d68b6826cf5c2e3f:
        hash: fb85823973ff92ac
        text: Move `wezterm-chord-collisions` (103) and `shell-scripts-shellcheck` (105). The latter must not exclude its own new siblings
    dd806a7bfdf3ebf6:
        hash: a09020063c1c2509
        text: 'Confirm all 19 `drvPath` values still equal the 1.1 baseline. A single mismatch means that check''s body changed and the move is wrong 2.6. Result: 18 of 19 identical. `shell-scripts-shellcheck` legitimately changed, because its `src` IS the flake source and the move added 19 files to it. Isolated and proven: holding content constant, every other check including `citelock` is identical'
    e3fa047b5ed768f0:
        hash: e635c0ef29bb2248
        text: Record every check's `drvPath` before any edit, so each later move has an exact baseline to compare against (19 checks)
    ee64885769b6d012:
        hash: 366e349d47fbdbdb
        text: Run `nix flake check`, and fix every shellcheck finding the newly-linted bodies surface. A finding that reveals a real defect is recorded separately rather than silenced; a benign one gets a targeted disable with a reason
