# Nix build outputs
result
result-*
# `result` is a Colchis Go package, so keep that exact source directory.
!pkgs/colchis/internal/result/
!pkgs/colchis/internal/result/**
**/dist/
switch.err

# Flake outputs
.direnv/

# Nix
**/nix-darwin-master/
**/*.old
nix.secrets.conf

# AI Tooling
.claude/
.serena
!AGENTS.md
.pi/

# `**/sgconfig.yml` and `ast-grep/` are gitignored globally, but here they are the
# rule library this repo ships and the project root `nix flake check` gates, and an
# untracked file is absent from the flake source.
!sgconfig.yml
!modules/home/programs/ast-grep/
