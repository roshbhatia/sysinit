{
  config,
  pkgs,
  lib,
  values,
  ...
}:
{
  # lv426 - Personal MacBook Pro (aarch64-darwin)
  # This configuration extends the base darwin configuration

  # Additional packages specific to lv426
  # Most configuration comes from values.nix
}
