#!/usr/bin/env bash
set -euo pipefail

# The hook holds no rules of its own. `hack/lint.sh` is the one list, so what
# runs here and what runs in CI cannot drift.
exec "$(git rev-parse --show-toplevel)/hack/lint.sh"
